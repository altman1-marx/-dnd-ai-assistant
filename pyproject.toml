[project]
name = "dnd-ai-assistant"
version = "0.1.0"
description = "DND-first AI tabletop assistant prototype."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

